package Attack;

import MyUnit.DSUnit;
import MyUnit.Unit;

import java.util.ArrayList;

public class RandomAttack extends Tactic{
    @Override
    public ArrayList<Unit> getRanger(int index) {
        DSUnit.getInstance().getLinkedLists().get(index).get

        for (int i = 0; i < DSUnit.getInstance().getLinkedLists().size(); i++) {
            DSUnit.getInstance().getLinkedLists().get(i+1);
        }
        return null;
    }
}
